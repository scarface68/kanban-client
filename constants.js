// Desc: Constants for the application
const HOST = process.env.REACT_APP_BACKEND_URL;
export const Routes = {
  AUTH: HOST + "auth",
  BOARD: HOST + "board",
  LIST: HOST + "list",
  CARD: HOST + "card",
};
